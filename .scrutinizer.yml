checks:
  go: true
build:
  nodes:
    analysis:
      environment:
        go:
          version: go1.15
      tests:
        override:
          - go-scrutinizer-run
          - golint-run
          - govet-run
filter:
  excluded_paths:
    - "*_test.go"
  dependency_paths:
    - "vendor/"
